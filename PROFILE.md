# Spocket

Agent-native deployment/hosting platform. An AI coding agent ships a folder over MCP and it runs 24/7 with HTTPS URLs and custom domains. Supports Discord/Slack/Telegram/Twitch bots, web apps/APIs, static sites, scrapers, queue consumers and cron jobs on Node 22 and Python 3.11. No CLI, Dockerfile, or YAML.

- **Provider:** https://www.spocket.dev
- **Documentation:** https://www.spocket.dev/documentation/quickstart
- **Base URL:** https://www.spocket.dev
- **Tags:** PaaS, application-hosting, bot-hosting, developer-tools, agent-infrastructure, MCP, deployment, serverless, always-on
- **Public API:** yes
- **Agent-native (MCP / llms.txt / skills):** yes
- **Confidence:** 95/100

## APIs

- **Spocket MCP Server** — Hosted MCP server (Streamable HTTP) with ~20 tools for deploying and managing always-on apps. Uses OAuth 2.1 with PKCE and dynamic client registration; usable by any account holder from Claude Code, Cursor, Codex, and other MCP clients. (`https://www.spocket.dev/api/mcp`)
- **Spocket Platform REST API** — White-label reseller/provisioning REST API for hosting apps from a customer backend. OAuth2 client-credentials auth; endpoints for apps, power control, logs, sub-accounts and account. Gated behind the paid Fleet plan. Documented in prose only; no machine-readable spec published. (`https://www.spocket.dev/api/v1`)

> Three catalogable surfaces confirmed: a hosted MCP server (https://www.spocket.dev/api/mcp, OAuth 2.1 + DCR), a detailed llms.txt, and a documented Platform REST API (https://www.spocket.dev/api/v1, OAuth2 client-credentials, gated behind the paid Fleet plan). No machine-readable contract (OpenAPI/AsyncAPI/GraphQL/Postman) was found at conventional paths. No separately published agent-skills artifact beyond the MCP tool set. There is no CLI; the MCP server is hosted publicly (not local). llms.txt has one internally inconsistent passage but the dominant documentation supports inbound HTTP and site hosting.

_Generated by apis.io from a community submission._